apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "okdp-server.fullname" . }}-test-connection"
  labels:
    {{- include "okdp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "okdp-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
