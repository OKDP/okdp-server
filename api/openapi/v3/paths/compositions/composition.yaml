get:
  summary: List all Compositions
  description: |
    todo
  tags:
    - compositions
  operationId: listCompositions
  parameters:
    - in: path
      name: spaceId
      schema:
        type: string
      required: true
      description: todo
  responses:
    '200':
      description: todo
      content:
        application/json:
          schema:
            $ref: '../../definition/Compositions.yaml'
    default:
      description: unexpected error
      content:
        application/json:
          schema:
            $ref: '../../definition/Error.yaml'

post:
  summary: Create a Compositions
  description: |
    todo
  tags:
    - compositions
  operationId: createComposition
  parameters:
    - in: path
      name: spaceId
      schema:
        type: string
      required: true
      description: todo
  requestBody:
    description: todo
    required: true
    content:
      application/json:
        schema:
          $ref: '../../definition/Composition.yaml'
  responses:
    '204':
      description: todo
    default:
      description: unexpected error
      content:
        application/json:
          schema:
            $ref: '../../definition/Error.yaml'

