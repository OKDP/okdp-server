get:
  summary: Get a space by spaceId
  description: |
    todo
  tags:
    - spaces
  operationId: getSpaces
  parameters:
    - in: path
      name: spaceId
      schema:
        type: string
      required: true
      description: todo
  responses:
    '200':
      description: todo
      content:
        application/json:
          schema:
            $ref: '../../definition/Spaces.yaml'
    default:
      description: unexpected error
      content:
        application/json:
          schema:
            $ref: '../../definition/Error.yaml'

delete:
  summary: Delete a space
  description: |
    todo
  tags:
    - spaces
  operationId: deletSpace
  parameters:
    - in: path
      name: spaceId
      schema:
        type: string
      required: true
      description: todo
  responses:
    '204':
      description: todo
    default:
      description: unexpected error
      content:
        application/json:
          schema:
            $ref: '../../definition/Error.yaml'
put:
  summary: Update a space
  description: |
    todo
  tags:
    - spaces
  operationId: updateSpace
  requestBody:
    description: todo
    required: true
    content:
      application/json:
        schema:
          $ref: '../../definition/Space.yaml'
  parameters:
    - in: path
      name: spaceId
      schema:
        type: string
        #format: uuid
      required: true
      description: todo
  responses:
    '204':
      description: todo
    default:
      description: unexpected error
      content:
        application/json:
          schema:
            $ref: '../../definition/Error.yaml'